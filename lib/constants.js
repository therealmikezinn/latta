const IDENTITY = Symbol("IDENTITY");

const MODULE_IDENTITY = Symbol("MODULE IDENTIFIER");

const IOC_IDENTITY = Symbol("IOC IDENTIFIER");

exports = module.exports = {
  IDENTITY,
  MODULE_IDENTITY,
  IOC_IDENTITY,
};

